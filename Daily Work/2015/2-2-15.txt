Added a check for TSC on the saveChangesBtn so that you can't save a TSC unless the EMPL_ID the user submitted is a correct id.

saveChangesBtn now changes html text depending on which tab you click.

Edited the JS so that the Edit TSCs Modal works as it should. I also added a new method to the controller to separate adding and editing TSCs.

Small min-height issue fixed in Edit Campus modal. Changed min-height from 4 em to 5.5 em

Made it so if TSC is inactive, they won't appear with their campuses on the first visible table on the page.

